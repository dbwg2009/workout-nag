FROM node:20-slim
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
RUN npm run build:worker

# Run migrations + seed, then start the bot.
CMD ["sh", "-c", "npm run db:migrate && npm run db:seed && node dist/worker/index.js"]
